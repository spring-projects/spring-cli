= boot add
Merge an existing project into the current Spring Boot project

== NAME
boot add

== SYNOPSIS
====
[source]
----
boot add --from String --path String --help 
----
====

== OPTIONS
`--from` String Add to project from an existing project name or URL *Optional*

`--path` String Path *Optional*

`--help or -h`  help for boot add *Optional*


include::footers/boot-add-footer.adoc[opts=optional]
